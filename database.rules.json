{
  "rules": {
    "matchmakingQueue": {
      ".read": "auth != null",
      ".write": "auth != null"
    },
    "playerBattles": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    }
  }
}
